# Elytras — dépendances cœur (mode fichier, sans base de données).
# Tout fonctionne avec ces seuls paquets : déploiement local en 1 commande.
fastapi==0.115.*
uvicorn[standard]==0.32.*
httpx==0.27.*
pydantic==2.*
cryptography>=42

# Lecture/extraction de contenu des fichiers par l'agent (factures, devis, étiquettes…).
pypdf==4.*               # texte des PDF
python-docx==1.*         # texte des Word .docx
openpyxl==3.*            # contenu des Excel .xlsx
pytesseract==0.3.*       # OCR (nécessite le binaire « tesseract-ocr », installé dans l'image Docker)
Pillow==10.*             # images pour l'OCR

# Postgres = OPTIONNEL (mémoire vectorielle à grande échelle, multi-process).
# Pour l'activer : pip install -r requirements-postgres.txt puis renseigner DATABASE_URL.
